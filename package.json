{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "cp .env.development .env && nuxt dev",
    "staging": "cp .env.staging .env && nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "axios": "^1.7.3",
    "module": "link:@nuxt/typescript-build/module",
    "nuxt": "^3.12.4",
    "vue": "latest",
    "vue-tsc": "^2.0.29"
  }
}
